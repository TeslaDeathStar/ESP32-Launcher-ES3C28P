#ifndef LAUNCHER_AMY_APP_H
#define LAUNCHER_AMY_APP_H

// Small, permission-gated companion client for the Morning Star gateway.
// It lives in Launcher itself, so selecting it does not replace the launcher
// with another firmware image.
void loopAmy();

#endif
